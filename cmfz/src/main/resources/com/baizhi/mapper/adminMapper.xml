<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.baizhi.dao.AdminDAO">
    <resultMap id="adminMap" type="Admin">
        <id column="admin_id" property="id"></id>
        <result column="username" property="username"></result>
        <result column="password" property="password"></result>
        <result column="salt" property="salt"></result>
        <collection property="role" ofType="Role">
            <id column="role_id" property="id"></id>
            <result column="role_name" property="name"></result>
            <collection property="permission" ofType="Permission">
                <id column="permission_id" property="id"></id>
                <result column="permission_name" property="name"></result>
            </collection>
        </collection>
    </resultMap>


    <select id="selectAll" resultType="Admin">
        select admin_id id,username,password from c_admin
    </select>

    <select id="selectByName" resultMap="adminMap">
        select a.admin_id id,a.username,a.password,a.salt,c.role_id,c.role_name,e.permission_id,e.permission_name
            from c_admin a
         right join c_admin_role b on a.admin_id=b.admin_id
         right join c_role c on b.role_id=c.role_id
         left join c_role_permission d on c.role_id=d.role_id
         left join c_permission e on d.permission_id=e.permission_id
         where username=#{username}
    </select>

</mapper>